{
  "name": "pagetomd",
  "private": true,
  "version": "3.3.0",
  "type": "module",
  "scripts": {
    "build": "pnpm run clean && vite build --config vite.content.config.js && vite build --config vite.background.config.js",
    "dev": "pnpm run clean && concurrently -k \"vite build --watch --config vite.content.config.js\" \"vite build --watch --config vite.background.config.js\"",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "defuddle": "^0.6.6",
    "turndown": "^7.2.2",
    "turndown-plugin-gfm": "^1.0.2",
    "webextension-polyfill": "^0.12.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "vite": "^7.1.3"
  }
}